#include <stdio.h>

main()
{
    /**
    1. int (for integer(whole numbers) data type ie.1, 2, 10 etc)
    2. float (for decimal data type)
    */
    
    int age=31;
    int year=1991;
    float GPA=4.0;
    
    printf("I am %d years old\n",age);
    printf("My year of birth is %d.\n",year);
    printf("My GPA is %1.1f.\n",GPA);
    return 0;
}